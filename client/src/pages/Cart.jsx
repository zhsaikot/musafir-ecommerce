import { Link } from 'react-router-dom'
import { ArrowRight, Minus, Plus, ShoppingBag, Trash2 } from 'lucide-react'
import useCartStore from '../store/cartStore'

const Cart = () => { const { items, addItem, removeItem, removeAll } = useCartStore(); const subtotal = items.reduce((total, item) => total + item.price * item.quantity, 0); if (!items.length) return <main className="min-h-screen bg-dark px-6 py-32 text-center text-white"><ShoppingBag className="mx-auto text-primary" size={48} /><h1 className="mt-6 text-4xl font-bold">Your bag is quiet.</h1><p className="mt-3 text-gray-400">Add something meaningful to get started.</p><Link to="/products" className="mt-8 inline-flex rounded-full bg-primary px-6 py-3 font-bold text-dark">Explore the collection</Link></main>; return <main className="min-h-screen bg-dark px-4 pb-24 pt-16 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[0.3em] text-primary">Your selection</p><h1 className="mt-4 text-5xl font-bold">Shopping bag</h1><div className="mt-12 grid gap-12 lg:grid-cols-[1fr_360px]"><div className="space-y-4">{items.map((item) => <div key={item.id} className="flex gap-4 border-b border-white/10 pb-5"><img src={item.image} alt={item.name} className="h-28 w-28 rounded-2xl object-cover" /><div className="flex flex-1 flex-col justify-between"><div className="flex justify-between gap-4"><div><h2 className="font-semibold">{item.name}</h2><p className="mt-1 text-sm text-gray-500">{item.color}</p></div><button onClick={() => removeAll(item.id)} className="hidden text-gray-500 hover:text-red-300 sm:block" aria-label={`Remove ${item.name}`}><Trash2 size={17} /></button></div><div className="flex items-center justify-between"><div className="flex items-center rounded-full border border-white/15"><button onClick={() => removeItem(item.id)} className="p-2 text-gray-400"><Minus size={14} /></button><span className="w-7 text-center text-sm">{item.quantity}</span><button onClick={() => addItem(item)} className="p-2 text-gray-400"><Plus size={14} /></button></div><span className="font-bold text-primary">৳{item.price * item.quantity}</span></div></div></div>)}</div><aside className="h-fit rounded-[1.5rem] border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-bold">Summary</h2><div className="mt-6 flex justify-between text-gray-400"><span>Subtotal</span><span>৳{subtotal}</span></div><div className="mt-3 flex justify-between text-gray-400"><span>Delivery</span><span>Calculated at checkout</span></div><div className="mt-6 flex justify-between border-t border-white/10 pt-5 text-xl font-bold"><span>Total</span><span className="text-primary">৳{subtotal}</span></div><Link to="/checkout" className="mt-7 flex items-center justify-center gap-2 rounded-full bg-primary px-5 py-4 font-bold text-dark">Checkout <ArrowRight size={17} /></Link></aside></div></div></main> }
export default Cart
