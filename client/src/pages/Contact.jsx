import { Mail, MapPin, MessageCircle, Phone } from 'lucide-react'
import { useState } from 'react'
import toast from 'react-hot-toast'

const Contact = () => { const [sent, setSent] = useState(false); const submit = (event) => { event.preventDefault(); setSent(true); toast.success('Message received. We will be in touch.'); event.target.reset() }; return <main className="min-h-screen bg-dark px-4 pb-24 pt-20 text-white sm:px-6 lg:px-8"><div className="mx-auto grid max-w-6xl gap-14 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="mb-4 text-xs font-bold uppercase tracking-[0.3em] text-primary">Get in touch</p><h1 className="text-5xl font-bold leading-tight">We are here to help.</h1><p className="mt-6 text-lg leading-8 text-gray-400">Questions about a product, an order, or finding the right gift? Send us a note and our team will reply within one business day.</p><div className="mt-12 space-y-6">{[[Mail, 'Email', 'hello@musafir.store'], [Phone, 'Phone', '+880 1700 000000'], [MapPin, 'Based in', 'Dhaka, Bangladesh']].map(([Icon, title, text]) => <div key={title} className="flex items-center gap-4"><Icon className="text-primary" size={21} /><div><p className="text-sm text-gray-500">{title}</p><p className="mt-1 text-gray-200">{text}</p></div></div>)}</div></div><form onSubmit={submit} className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-6 sm:p-10"><div className="mb-8 flex items-center gap-3"><MessageCircle className="text-primary" /><h2 className="text-2xl font-bold">Send a message</h2></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Name" name="name" required /><Field label="Email" name="email" type="email" required /></div><Field label="Subject" name="subject" required /><label className="block text-sm text-gray-400">Message<textarea name="message" rows="6" required className="mt-2 w-full rounded-xl border border-white/10 bg-dark p-4 text-white outline-none focus:border-primary" /></label><button className="mt-6 w-full rounded-full bg-primary px-6 py-4 font-bold text-dark transition hover:bg-primary-light">{sent ? 'Message sent' : 'Send message'}</button></form></div></main> }
const Field = ({ label, name, type = 'text', required }) => <label className="mb-5 block text-sm text-gray-400">{label}<input name={name} type={type} required={required} className="mt-2 w-full rounded-xl border border-white/10 bg-dark p-4 text-white outline-none focus:border-primary" /></label>
export default Contact
